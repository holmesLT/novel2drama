# 剧本数据格式（v0.1）

这是整个 novel2drama 项目的**中间数据格式**：第一步的输出、第二步（剧本 → 分镜）的输入。
它是一个 JSON 文件，结构如下。

## 顶层字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `title` | string | 本集标题 |
| `genre` | string | 题材类型，如「都市」「玄幻」「悬疑」 |
| `characters` | array | 角色表，后续生成角色立绘/分镜时会用到 |
| `scenes` | array | 场景列表，按播放顺序排列 |

## characters 元素

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `name` | string | 角色名（全剧唯一） |
| `desc` | string | 一句话外貌/性格描述，用于后续文生图 |

## scenes 元素

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `scene_id` | number | 场景编号，从 1 开始递增 |
| `heading` | string | 场景标题，格式固定为 `内景/外景 - 地点 - 日/夜`，例：`内景 - 旧书店 - 夜` |
| `summary` | string | 本场景剧情概要（一句话） |
| `beats` | array | 节拍列表，即按顺序发生的动作与台词 |

## beats 元素（两种类型）

**动作节拍**（对应画面/镜头描述）：

```json
{"type": "action", "text": "特写：雨点砸在书店褪色的招牌上，玻璃门被推开，风铃乱响。"}
```

**台词节拍**：

```json
{"type": "dialogue", "character": "林晚", "text": "我要找一本书。", "emotion": "冷静"}
```

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `type` | string | 是 | `action` 或 `dialogue` |
| `text` | string | 是 | 动作描述 / 台词内容 |
| `character` | string | dialogue 必填 | 必须出现在角色表中 |
| `emotion` | string | 否 | 情绪提示，供 TTS 配音使用 |

## 设计原则

1. **每一步都可人工编辑**：直接改 JSON（或改 md 再让我同步）即可。
2. **字段向后兼容**：第二步会增加 `camera`（镜头）字段，但不会改动现有字段的含义。
3. **`heading` 是机器可解析的**：第二步据此判断内景/外景、日夜，来选择生成风格。
